apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service
  namespace: dev
spec:
  imagePullSecrets:
    - name: regcred
  replicas: 8
  selector:
    matchLabels:
      app: profile-service
  template:
    metadata:
      labels:
        app: profile-service
    spec:
      containers:
        - name: profile-service
          image: "{{ .Values.profileService.image }}"
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: dev
spec:
  selector:
    app: profile-service
  ports:
    - port: 3000
      targetPort: 3000
